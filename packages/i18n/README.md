# @huliho/i18n

Message catalogs and the i18n runtime setup, consumed by every app in
the workspace. The package starts empty.

`pnpm build` at the repo root compiles it; unit tests run with
`pnpm test`.
