# @huliho/core

Domain types, the JMAP client and sync logic, free of React and DOM
APIs so every client can share it. The package starts empty.

`pnpm build` at the repo root compiles it; unit tests run with
`pnpm test`.
