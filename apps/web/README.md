# @huliho/web

The Huliho web client: a React SPA built with Vite, with the React
Compiler enabled.

From the repo root: `pnpm build` builds it, `pnpm test` runs the unit
tests and `pnpm test:e2e` runs the Playwright smoke test. `pnpm dev`
inside this directory starts the dev server.
