# huliho-server

The Huliho server binary. It serves the built web app and the API from
one process; the HTTP skeleton is not built yet, so the binary does
nothing today.

Build and test from the workspace root: `cargo build` and
`cargo test --workspace`.
